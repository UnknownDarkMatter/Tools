====================================================================
bash commandes
====================================================================
history

====================================================================
/etc/bash.bashrc 
====================================================================
alias my_menu='echo "my_menu_edit **** my_go_to_dheu my_go_to_dheu_dev"'
alias my_menu_edit='nano ~/.bashrc'
alias ll='ls -l'
alias la='ls -A'
alias l='ls -CF'
alias lla='ls -lA'
alias my_go_to_dheu='cd "/mnt/d/DavidHartmannEU"'
alias my_go_to_dheu_dev='cd "/mnt/d/DavidHartmannEU/DEV/"'
screenfetch -n

====================================================================
vim commandes
====================================================================
:Ex
:!command pour executer de la ligne de commande , ex : touch newfile.txt
==========================================================================================
vim (https://www.tonybtw.com/tutorial/vim/) / " echom "hello world" pour tester un fichier
==========================================================================================
==> .vimrc => first file read by vim
source ~/.vim/options.vim
source ~/.vim/keybinds.vim
source ~/.vim/plugins.vim

==> ~/.vim/options.vim

filetype plugin indent on
syntax on

set expandtab
set shiftwidth=4
set softtabstop=4
set tabstop=4

set number
set relativenumber
set smartindent

set showmatch "show Matching brackets.

set  cursorline
set  cursorcolumn
autocmd VimEnter * highlight CursorLine guibg=orange guifg=black
autocmd VimEnter * highlight CursorColumn guibg=orange guifg=black
autocmd InsertEnter * highlight CursorLine guibg=green guifg=black
autocmd InsertEnter * highlight CursorColumn ctermfg=White ctermbg=Yellow cterm=bold guifg=white guibg=green gui=bold
autocmd InsertLeave * highlight CursorLine guibg=orange guifg=black
autocmd InsertLeave * highlight CursorColumn ctermfg=Black ctermbg=Yellow cterm=bold guifg=Black guibg=white gui=NONE

set backspace=indent,eol,start
set fileformats=dos,unix

set backup
if !isdirectory($HOME."/.vim/backupdir")
    silent! execute "!mkdir ~/.vim/backupdir"
endif
set backupdir=~/.vim/backupdir
"not generate .swap
set noswapfile


=> ~/.vim/keybinds.vim
map <C-o> :NERDTreeToggle<CR>

=> ~/.vim/plugins.vim
let s:plugin_dir = expand('~/.vim/plugged')

function! s:ensure(repo)
  let name = split(a:repo, '/')[-1]
  let path = s:plugin_dir . '/' . name

  if !isdirectory(path)
    if !isdirectory(s:plugin_dir)
      call mkdir(s:plugin_dir, 'p')
    endif
    execute '!git clone --depth=1 https://github.com/' . a:repo . ' ' . shellescape(path)
  endif

  execute 'set runtimepath+=' . fnameescape(path)
endfunction

call s:ensure('preservim/nerdtree')
call s:ensure('gcmt/taboo.vim')
call s:ensure('vim-airline/vim-airline')
call s:ensure('pangloss/vim-javascript')
call s:ensure('ghifarit53/tokyonight-vim')

" tokyonight vim
set termguicolors
let g:tokyonight_style = 'night' " available: night, storm
let g:tokyonight_enable_italic = 1
colorscheme tokyonight


=====================================================================================
tmux
=====================================================================================
nano ~/.tmux.conf

# Pane borders
set -g pane-border-style fg=blue
set -g pane-active-border-style fg=yellow

# Status bar
set -g status-style bg=black,fg=white

# Window title
set -g window-status-current-style bg=default,fg=green,bold

Window Management
Ctrl+b c	Create window
Ctrl+b w	List windows
Ctrl+b n	Next window
Ctrl+b p	Previous window
Ctrl+b 0-9	Switch to window #
Ctrl+b ,	Rename window
Ctrl+b &	Kill window

Pane Management
Ctrl+b %	Split horizontal
Ctrl+b "	Split vertical
Ctrl+b ←↑→↓	Navigate panes
Ctrl+b o	Cycle panes
Ctrl+b z	Zoom pane
Ctrl+b Space	Cycle layouts
Ctrl+b x	Kill pane

session
tmux new -s nom-session
Ctrl+b D détacher la session
tmux attach -t nom-session : pour s'attacher à une session
tmux ls : session active
tmux switch -t nom-dession : bascule de session
tmux kill-session -t nom-session
